# Boost unit suites that are expected to fail, one per line.
#
# Updated 2026-09-23, after mainnet powLimit was corrected and the placeholder
# genesis re-mined at the launch difficulty. That took the suite from
# 120 pass / 23 fail to 134 pass / 9 fail, with no new failures.
#
# The previous version of this file asserted that every entry failed for a
# single reason -- the mainnet genesis being mined at nBits 0x207fffff, easier
# than mainnet's own powLimit, so it never connected and any suite building a
# mainnet chainstate asserted on a null tip. That held for 14 of the 23, which
# now pass. It did not hold for the 9 below: they were failing at genesis
# first, which masked unrelated problems underneath.
#
# Each remaining entry and where it actually fails:
#
#   blockmanager_tests          blockmanager_flush_block_file -- block file
#                               flush accounting
#   denialofservice_tests       stale_tip_peer_management -- connman state
#   miner_tests                 CreateNewBlock_validity -- TestBlockValidity
#                               throws; the one most likely to be a real
#                               product issue rather than stale test data
#   net_peer_connection_tests   addnode/getaddednodeinfo connection handling
#   psbt_wallet_tests           import_descriptor asserts descs.size() == 1
#   rpc_tests                   rpc_rawsign -- "Invalid TrueNorth address: 3H..."
#                               an inherited Bitcoin P2SH test vector
#   validation_tests            subsidy_limit_test -- asserts nSubsidy <= 50 *
#                               COIN; TrueNorth's initial subsidy is 512 COIN.
#                               Inherited Bitcoin expectation, not a defect.
#   wallet_tests                scan_for_wallet_transactions -- GetBalance
#   walletload_tests            wallet_load_descriptors -- LoadWallet
#
# At least two (rpc_tests, validation_tests) are stale Bitcoin test data rather
# than product defects and should be straightforward to correct. miner_tests
# deserves looking at properly before launch.
#
# test/run-unit-suites.sh compares in both directions, so CI fails and tells
# you to delete entries as they start passing. When this file is empty, the
# script can be replaced with a single test_bitcoin run.
#
# Note: that script needs bash 4+ (mapfile) and will not run on macOS's
# bash 3.2. CI is Linux, so this only affects local runs.

blockmanager_tests
denialofservice_tests
miner_tests
net_peer_connection_tests
psbt_wallet_tests
rpc_tests
validation_tests
wallet_tests
walletload_tests
